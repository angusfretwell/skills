Every slice has landed on branch {{BRANCH}}. Typecheck once and fix only what the integration of slices broke — `/commit` and push if you changed anything. Refresh the draft PR's title and body with `/open-pr` so they describe the whole branch; keep `Closes {{ISSUE_ID}}` and keep it a draft.
Outcome: `{ "result": "FINISHED" }`.
